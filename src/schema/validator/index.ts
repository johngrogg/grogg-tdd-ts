// TODO: implement this using some third party JSON schema validation package

export default class Validator {
  constructor() {}

  public validate(schemaString: string): boolean {
    return true;
  }
}
